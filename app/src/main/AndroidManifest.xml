<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.Coinnect"
    tools:targetApi="31">
    <activity
      android:exported="false"
      android:name=".activities.pending.PendingTransactionsActivity" />
    <activity
      android:exported="false"
      android:name=".activities.login.UsernameSignInActivity" />
    <activity
      android:exported="false"
      android:name=".activities.transactions.addTransaction.AddTransactionActivity" />
    <activity
      android:exported="false"
      android:name=".activities.login.RegisterActivity" />
    <activity
      android:exported="false"
      android:name=".activities.transactions.TransactionsActivity" />
    <activity
      android:exported="false"
      android:name=".activities.settings.SettingsActivity" />
    <activity
      android:exported="false"
      android:name=".activities.friends.FriendsActivity" />
    <activity
      android:exported="false"
      android:name=".activities.welcome.WelcomeActivity" />
    <activity
      android:exported="false"
      android:name=".activities.home.HomeActivity" />
    <activity
      android:exported="true"
      android:name=".activities.login.LoginActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

  <uses-permission android:name="android.permission.INTERNET" />

</manifest>