<resources>
  <!-- Base application theme. -->
  <style name="Base.Theme.Coinnect" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- Customize your light theme here. -->
    <item name="colorPrimary">@color/actualBlack</item>
    <item name="colorSecondary">@color/white</item>
    <item name="colorAccent">@color/accentGreen</item>

  </style>

  <!-- sets color of the navbar tint to an accent green. -->
  <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
    <item name="android:color">@color/accentGreen</item>
  </style>

  <style name="Theme.Coinnect" parent="Base.Theme.Coinnect" />
</resources>